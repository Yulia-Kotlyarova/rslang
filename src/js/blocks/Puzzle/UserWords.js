export default class UserWords {
  constructor(userWords) {
    this.userWords = userWords;
    this.userWordsLength = 0;
  }

  // getUserWords() {
  //   this.userWords = this.userWords.filter((word) => word.textExample.split(' ').length < 11);
  //   if (this.userWords.length < 10) {
  //   }
  // }
}
